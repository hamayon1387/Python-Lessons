# Rules for choosing an identifier:
# . A varible can cantain alphabets, digits, and underscores.
# . A varible name can only start with an alphabet and underscore.
# . A varible name can't be start with a digit and number.
# . No white space is allowed to be used inside varible name.

# Examples of a few varible names are: hamayon, one8, seven_, _seven and etc...


number1 = 12 # We can use numbers in the end of identifiers.
number2 = 23 # It's an valid identifier.

name = "hamayon" # We can use from alphabets for mading identifiers.
_fname = "ahmad" # We can use from underscores in the begining of an identifier and inside an identifier. like following varible: 
mother_name = "fatema" # It's a valid identifier
# 1name = "haroon" # It's invalid identifier because we can't use from numbers in the begining of an idetifier.
# @email = "hamayon@gmail.com" # An identifier can't be have a digit.
# user@name = "hamayon1387" # it's invalid identifier because python identifier can't ber identify when it want to use in a program. like in following program:
# console.log(user@name) 
userName = "hamayon17" # We can use from camel case.
user_name = "hamayon" # The most popular syntax for identifing a containers like Varibles, Functions and objects ...


print(number1, number2, _fname, name, userName, user_name)
print(mother_name)