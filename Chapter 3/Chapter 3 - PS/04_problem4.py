# Write a Python program to replace double space to a single space in a sentence.
sentence = "I  love  you  Python"
print(sentence)
print(sentence.replace("  ", " ")) # Strings are immutable which means that you cannot change them by running functions on them.